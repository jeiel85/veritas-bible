# 변경 기록

## Unreleased - 본문 가독성·언어·연구 범위 UX 보강

- **영어 모드에서 책 이름 영어 표시**: `BibleViewModel` 이 DB 카탈로그(`BookCatalogEntry` 66권)를 메모리에 캐시. 리더 헤더·드롭다운·메모 다이얼로그·연구 세션 헤더·세션 카드 모두 `appLanguage="EN"` 일 때 자동으로 `John / Genesis / Romans …` 로 노출.
- **본문 마킹 시각화 재설계**: 색상 배경 블록 → **타입별 색상 + 두꺼운 밑줄 + SemiBold**. 문장 흐름이 끊기지 않아 한국어 본문 가독성이 살아남.
- **구조 연결선 Canvas overlay**: 같은 절 안의 명시 link 는 `TextLayoutResult.getBoundingBox` 로 두 마킹의 중심을 잡아 본문 아래에 brace(`└─┘`) 곡선을 그리고, 줄을 넘는 link 는 양쪽에 짧은 끝꼬리(↳, ↰)로 표시. 색상은 from 마킹의 타입 색을 따라간다.
- **연구 범위 직접 선택(여기부터 / 여기까지)**: 새 `StudyCreateDialog` 가 두 anchor 카드(`여기부터` / `여기까지`)로 시작/끝을 분리. 각 anchor 를 탭하면 책 → 장 → 절 3단계 picker(`AssistChip` 인디케이터 + LazyColumn / Grid)가 열려 직접 선택. start > end 가 되면 자동으로 정렬 + 경고 안내.
- **한글 개역 단락 ● 표시**: `scripts/build_bible_json.py` 가 영어 WEB USFM 의 `\p / \q / \pi / \pm …` paragraph 마커를 감지해 14,676개 절에 `pb: true` 를 부여. 리더와 연구 상세 본문 패널이 단락 시작 절 앞에 `●` 를 그려 한국어 사용자가 익숙한 단락 감각을 그대로 살림.

### 데이터·안정성

- Room DB **v5 → v6** 마이그레이션: `bible_verses.paragraphStart INTEGER NOT NULL DEFAULT 0`. 기존 사용자의 하이라이트·메모는 그대로 보존되며, `BibleDataPrepopulator.syncParagraphMarksIfNeeded` 가 앱 첫 실행 시 9.7MB `bible.json` 의 `pb` 정보만 UPDATE 로 반영.
- v5↔v6 마이그레이션 자동 테스트(`StudyMigrationTest`) 1건 추가, 기존 v1↔v5 5건과 함께 전구간 무손실 검증.

### 국제화

- 신규 i18n 키 (KO/EN 동시): `study_create_dialog_hint`, `study_create_anchor_start/end`, `study_create_invalid_range`, `study_create_step_book/chapter/verse`, `study_create_picker_confirm`.

## v1.6.0 - 2026-05-24 (귀납적 성경연구 워크벤치)

### 신규 기능

- **‘성경연구’ 탭 신설**: 하단 네비게이션에 추가되었으며, 기존 5개 탭(성경읽기/검색/통계분석/연구노트/설정백업)은 그대로 유지됩니다.
- **본문에서 연구 시작**: 성경 리더의 절 선택 메뉴에 ‘이 본문으로 귀납적 연구 시작’ 버튼이 추가되어, 현재 본문 범위를 그대로 가져와 새 연구 세션을 만들 수 있습니다.
- **9단계 연구 워크플로우**: 관찰 → 마킹/구조 → 단락 → 해석 → 핵심주제(검증 체크리스트 5종) → 명제 → 개요 → 적용 → 리포트의 9개 탭으로 모든 단계가 완성되었습니다.
- **본문 마킹 18종**: 주어/동사/목적어/접속사/반복어/핵심어/명령/요청/설명/서술/원인/결과/대조/목적/조건/근거/정의/적용 단서. 모바일 단어 칩 선택 다이얼로그로 절 단위 마킹.
- **구조 연결 9종**: 주어↔동사 등 9가지 연결 타입. ‘주어 → 동사 흐름’ 요약 화면 제공.
- **관찰 질문 템플릿 8종**: 반복어/주체/주요 동사/말투/접속사/원인-결과/대조/결론. 질문마다 단일 답안 자동 저장.
- **성격 태그 14종**: 단락마다 정의/의미/이유/원인/결과/목적/조건/과정/방식/특징/근거/관계/대조/적용 태그 부착.
- **해석 노트**: 핵심 단어/평이한 의미/문맥 의미/본문 근거/관련 구절/해석 결론을 카드 단위로 기록·수정.
- **핵심주제 검증 체크리스트**: 본문 전체 포괄 / 단락 뒷받침 / 본문 근거 / 적용-해석 순서 / 선입견 점검 5가지 기준에 메모와 함께 체크.
- **명제·개요**: 검토 상태 3종(초안/검토 완료/수정 필요) 지원, 부모-자식 개요 트리 구조.
- **적용 노트 고도화**: Material 3 DatePicker로 점검일 선택 + 실천 여부 토글.
- **Markdown / PDF 리포트**: ‘Markdown 생성’ → 미리보기 → 복사 / 공유 / **PDF 로 공유** / **시스템 인쇄**(설치된 모든 print service). PDF는 외부 라이브러리 없이 Android 표준 `PdfDocument` 사용.
- **명시적 본문 근거 링크**: 해석은 마킹 1개, 명제는 마킹·단락 다중 선택. 카드에 MarkupTheme 색상 칩으로 시각화.
- **암호화 백업 확장**: 기존 백업/복원에 모든 연구 데이터(11개 테이블) 포함. 복원 시 ‘기존 study 데이터 덮어쓰기’ 토글 제공.

### 데이터·안정성

- Room DB v1 → v5 단계별 마이그레이션. 기존 사용자의 메모/북마크/읽기 기록/목표는 손실 없이 보존됩니다.
- 마킹·단락 삭제 시 stale 링크(해석/명제/개요)를 자동 정리하는 sweeper.
- v1~v5 전구간 마이그레이션 자동 테스트(Room `MigrationTestHelper`) 도입.

### 국제화·접근성

- 신규 문자열 모두 한국어/영어 동시 추가(`LanguageManager`). 리포트 라벨도 사용자 언어 설정을 따릅니다.
- 색상에만 의존하지 않도록 마킹 타입을 항상 텍스트 라벨과 함께 표시.

### 정책

- 외부 AI API, 외부 서비스, 광고 SDK 추가 없음. 모든 연구 데이터는 로컬 우선.

## v1.0.0 - 2026-05-22 (Android 리뉴얼 이관)

- `D:\Project\veritas-bible-renew` 기반 Android/Kotlin Compose 리뉴얼 프로젝트로 전환.
- 기존 Flutter 기반 구조 제거.
- 온보딩, 대시보드, 성경 리더, 검색, 노트, 설정 화면을 포함한 리뉴얼 앱 구조 반영.
- Gradle Wrapper 추가 및 Android 명령줄 빌드 검증 환경 정비.
- 태그 기반 GitHub Release 및 Play Console 릴리즈 노트 관리 흐름 추가.
